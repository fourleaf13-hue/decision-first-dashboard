# Radar visual refresh

Approved direction: keep the decision-first radar semantics already agreed (40/60/80/100 quantitative tiers, empty center, previous period in blue beneath current period in pink, labels outside the 100% ring with symmetric spacing and safe-zone protection) while adopting the visual depth system from the supplied SVG reference.

The reference is used for visual language only, not copied as product chrome. The renderer should use a soft gray page field, one ambient field below all content, four opaque white floating data tiers with distinct soft shadows, thin neutral scale boundaries, exact comparison colors (#4D82E2 previous, #ED69AD current, #FF7BC2 at 10% current fill), and lighter support cards. SVG and HTML outputs must remain deterministic and data-grounded.
